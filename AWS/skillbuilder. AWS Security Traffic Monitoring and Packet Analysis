1. What is the primary purpose of configuring a traffic mirror filter in AWS?
 (v)1. To select specific traffic for mirroring
    2. To compress network data
    3. To block unwanted network traffic
    4. To encrypt network traffic

2. What does the 'Session number' represent in a traffic mirror session configuration?
    1. The duration of the mirroring session
    2. The number of mirror targets
    3. The number of packets to mirror
 (v)4. The priority of the session

